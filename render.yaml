services:
  - type: web
    name: student-form-app-1-hksl
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    rootDir: client
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
